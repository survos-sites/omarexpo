<figure>

    <figcaption>{{ caption|default(audio.uri) }}:</figcaption>
    <audio
           controls
            src="{{ audio.uri }}">
        Your browser does not support the <code>audio</code> element.
    </audio>
</figure>
